import module1  # get module as a whole

if __name__ == '__main__':
    print('code snippets from page 713\n')
    # NOTE: when importing an entire module, it is necessary to qualify the
    # name of the component you wish to in order to access it
    module1.printer('Hello world!')
